# Gantt Report Feature Specification

## 📋 문서 개요

### 목적
Gantt Chart Report의 **기능 요구사항** 정의

### 전제 조건
- 이 문서는 `SPEC_UI.md`에 정의된 UI/UX 디자인을 이해했다는 것을 전제로 합니다
- Option B (탭 레이아웃) 방식을 기반으로 합니다

### 문서 버전
- **Version**: 1.0
- **작성일**: 2025-12-04
- **기반 문서**: SPEC_UI.md (Option B)

---

## 📐 화면 구조 (요약)

```
┌─────────────────────────────────────────┐
│           Header (Report Title)          │
├─────────────────────────────────────────┤
│    1. Summary Section (Rich Text)       │
├─────────────────────────────────────────┤
│    2. Gantt Configuration (탭)           │
│       [Timeline] [Structure] [Attributes]│
├─────────────────────────────────────────┤
│    3. Search Filter Section (NEW)       │
├─────────────────────────────────────────┤
│    4. Gantt Chart Section                │
└─────────────────────────────────────────┘
```

---

## 🎯 기능 요구사항

---

## 1. Summary Section

### 1.1 Rich Text Editor 적용

#### 요구사항
- Summary 섹션에 **Rich Text Editor** 기능 제공
- 사용자가 다양한 텍스트 서식을 적용할 수 있어야 함

#### 필수 기능
- **텍스트 서식**:
  - 굵게 (Bold)
  - 기울임 (Italic)
  - 밑줄 (Underline)
  - 취소선 (Strikethrough)

- **텍스트 정렬**:
  - 왼쪽 정렬
  - 가운데 정렬
  - 오른쪽 정렬

- **리스트**:
  - 순서 있는 목록 (Ordered List)
  - 순서 없는 목록 (Unordered List)

- **기타**:
  - 링크 삽입
  - 폰트 크기 조정
  - 색상 변경 (옵션)

#### 권장 라이브러리
- Quill.js
- TinyMCE
- Draft.js
- 또는 동등한 기능을 제공하는 에디터

---

## 2. Gantt Configuration Section

---

### 2.1 Timeline 탭 (X축 설정)

#### 개요
- Gantt Chart의 **X축(시간축)** 설정
- Timeline 설정에 따라 Gantt Chart의 X축이 동적으로 변경되어야 함

#### 2.1.1 기본 설정
- **기본 단위**: 년도 (Year)
- **기본 표시**: 분기(Quarter) 표시

#### 2.1.2 Year Range (년도 범위)

**기능**:
- 시작 년도 선택 (Dropdown)
- 종료 년도 선택 (Dropdown)
- 시작 년도 ≤ 종료 년도 검증

**동작**:
- 선택된 년도 범위에 따라 Gantt Chart X축 범위 자동 조정

**기본값**:
- 시작: 2022
- 종료: 2026

#### 2.1.3 Display Options (표시 옵션)

**옵션 1: Show Quarters (분기 표시)**
- 체크박스
- 기본값: ☑ 체크됨
- 활성화 시: X축에 Q1, Q2, Q3, Q4 표시
- 비활성화 시: 분기 표시 제거

**옵션 2: Show Months (월 표시)**
- 체크박스
- 기본값: ☐ 체크 해제
- 활성화 시: X축에 1월~12월 표시
- 비활성화 시: 월 표시 제거

**동시 활성화**:
- 분기와 월을 모두 표시 가능
- 계층적으로 표시: 년도 > 분기 > 월

#### 2.1.4 실시간 적용
- 설정 변경 시 **즉시** Gantt Chart X축에 반영
- 애니메이션 효과 권장 (부드러운 전환)

---

### 2.2 Structure 탭 (Y축 설정)

#### 개요
- Gantt Chart의 **Y축(그룹/계층)** 설정
- Structure 설정에 따라 Task들이 자동으로 그룹화되고 위치가 조정됨

#### 2.2.1 UI 변경: 입력 필드 → 테이블

**기존 (SPEC_UI.md)**:
```
┌──────────────────────────────────────┐
│ 1. Project                            │
│ 2. Phase                              │
│ 3. Task                               │
└──────────────────────────────────────┘
```

**변경 후 (테이블 형식)**:
```
┌─────────────────────────────────────────────────────┐
│ Depth Groups (Row)                            [Add]  │
├─────────────────────────────────────────────────────┤
│ Row 1: Group A                                [Del]  │
│  ├─ Depth 1: [Project ▼]                            │
│  ├─ Depth 2: [Phase ▼]                              │
│  └─ Depth 3: [Task ▼]                               │
├─────────────────────────────────────────────────────┤
│ Row 2: Group B                                [Del]  │
│  └─ Depth 1: [Owner ▼]                              │
└─────────────────────────────────────────────────────┘
```

#### 2.2.2 Row (그룹) 개념

**정의**:
- 1개 Row = 1개 Depth 그룹
- 각 Row는 독립적인 Y축 그룹을 형성
- Row 추가 = 새로운 Gantt Y축 그룹 추가

**특징**:
- 최소 Row 개수: 1개
- 최대 Row 개수: 제한 없음 (권장: 5개 이하)
- Row별로 이름 지정 가능 (예: "Group A", "Group B")

#### 2.2.3 Depth (계층) 설정

**제약 조건**:
- 각 Row 내에서 **최소 1개, 최대 8개** Depth 설정 가능
- Depth는 **Task의 속성(Attribute)** 중에서만 선택 가능

**입력 방식**:
- Dropdown 선택
- 선택 가능한 값: Task의 속성 목록
  - 예: Project, Phase, Task, Owner, Status, Priority, Team, etc.

**Depth 순서**:
- Depth 1 (최상위) → Depth 2 → ... → Depth 8 (최하위)
- 순서에 따라 계층 구조 형성
- 드래그로 순서 변경 가능 (옵션)

#### 2.2.4 동작 원리

**단일 Row (그룹) 예시**:
```
Row 1: 프로젝트별 구조
  Depth 1: Project
  Depth 2: Phase
  Depth 3: Task

→ Gantt Y축:
  ├─ Project A
  │   ├─ Phase 1
  │   │   ├─ Task 1
  │   │   └─ Task 2
  │   └─ Phase 2
  │       └─ Task 3
  └─ Project B
      └─ Phase 1
          └─ Task 4
```

**다중 Row (그룹) 예시**:
```
Row 1: 프로젝트별 구조
  Depth 1: Project
  Depth 2: Phase

Row 2: 담당자별 구조
  Depth 1: Owner
  Depth 2: Status

→ Gantt Y축 (분할 표시):
  [Row 1: 프로젝트별]
  ├─ Project A
  │   ├─ Phase 1
  │   └─ Phase 2
  └─ Project B
      └─ Phase 1

  [Row 2: 담당자별]
  ├─ John
  │   ├─ In Progress
  │   └─ Done
  └─ Sarah
      └─ In Progress
```

#### 2.2.5 동일 Item 처리

**규칙**:
- 여러 Row에서 **동일한 속성값을 가진 Task**는 각 그룹에 따로 표시
- 예: "Project A - Phase 1 - Task 1" Task가:
  - Row 1 (Project > Phase > Task)에 한 번
  - Row 2 (Owner > Status)에 한 번 (Owner가 John, Status가 In Progress라면)

#### 2.2.6 Row 관리 기능

**추가**:
- [+ Add Row] 버튼
- 클릭 시 새 Row 추가 (기본 Depth 1개)

**삭제**:
- 각 Row에 [Delete] 버튼
- 최소 1개 Row는 유지 (마지막 Row는 삭제 불가)

**이름 변경**:
- Row 이름 클릭 시 인라인 편집

#### 2.2.7 실시간 적용
- Depth 변경 시 **즉시** Gantt Chart Y축 재구성
- Task들이 자동으로 새로운 위치로 이동
- 애니메이션 효과 권장

---

### 2.3 Attributes 탭 (Task 표현 설정)

#### 개요
- Gantt Chart의 **Task 도형**에 표시될 속성(Attribute) 설정
- 사용자가 원하는 속성을 선택하고 시각적 표현을 커스터마이징

#### 2.3.1 UI 구조

```
┌───────────────────────────────────────────────────────┐
│ Task Attributes Table          │  Preview              │
│                                │                       │
│ ┌────┬─────────┬──────┐       │  ┌─────────────────┐ │
│ │ #  │ 속성    │ 라벨 │       │  │   Task 도형     │ │
│ ├────┼─────────┼──────┤       │  │                 │ │
│ │ ①  │ Owner   │ [..] │       │  │  ① John         │ │
│ │ ②  │ Status  │ [..] │       │  │  ② In Progress  │ │
│ │ ③  │ Priority│ [..] │       │  │                 │ │
│ └────┴─────────┴──────┘       │  └─────────────────┘ │
│                                │                       │
│ Shape: [Gantt Bar ▼]           │  Color: [Blue ▼]     │
└───────────────────────────────────────────────────────┘
```

#### 2.3.2 속성 테이블 (좌측)

**구성 요소**:
1. **번호 (순서)**: ①, ②, ③, ... (파란 동그라미)
2. **속성 이름**: Owner, Status, Priority, etc.
3. **라벨 텍스트**: 사용자가 편집 가능한 텍스트박스

**속성 라벨 형식**:
```
┌──────────────────────┐
│ ① [John         ]    │  ← ①: 파란 동그라미 + 번호
│                      │     [John]: 텍스트박스
└──────────────────────┘
```

**기능**:
- **속성 선택**: 체크박스로 표시할 속성 선택 (최대 8개)
- **라벨 편집**: 텍스트박스 클릭하여 라벨 이름 수정
- **순서 변경**: 드래그로 속성 순서 조정 (옵션)
- **추가/삭제**: 속성 추가/제거 가능

**초기 선택 속성** (기본 3개 체크):
- ☑ Owner
- ☑ Status
- ☑ Priority

**추가 가능 속성** (예시):
- ☐ Progress
- ☐ Budget
- ☐ Resource
- ☐ Tags
- ☐ Notes

#### 2.3.3 미리보기 (우측)

**목적**:
- 실제 Gantt Task가 어떻게 표시될지 실시간 미리보기
- 속성 라벨의 위치를 시각적으로 조정

**기능**:

**1) 속성 라벨 표시**:
- 테이블에서 체크된 속성들이 Task 도형 중앙에 자동 배치
- 초기 위치: 도형 중앙, 세로로 정렬

**2) 속성 라벨 이동**:
- 사용자가 마우스로 **드래그하여 임의의 위치로 이동** 가능
- 도형 내부 어디든 배치 가능
- 실시간으로 위치 업데이트

**3) 도형 경계**:
- 속성 라벨이 도형 밖으로 나가면 경고 또는 자동 스냅

**예시**:
```
초기 상태:
┌─────────────────┐
│                 │
│   ① Owner       │  ← 중앙 배치
│   ② Status      │
│   ③ Priority    │
│                 │
└─────────────────┘

사용자 조정 후:
┌─────────────────┐
│ ① Owner         │  ← 좌상단
│                 │
│         ② Status│  ← 우측
│ ③ Priority      │  ← 좌하단
└─────────────────┘
```

#### 2.3.4 Task 도형 설정

**1) 도형 형태 선택**:

Dropdown으로 선택:
- **Gantt Bar** (기본) - 가로 막대 형태
- **Circle** (동그라미) - ⭕
- **Rectangle** (네모) - ▢
- **Triangle** (세모) - △
- **Diamond** (다이아몬드) - ◇ (옵션)

**2) 도형 색상 선택**:

Dropdown 또는 Color Picker:
- 검은색 (#000000)
- 회색 (#808080)
- 파란색 (#3B82F6)
- 빨간색 (#EF4444)
- 노란색 (#FACC15)
- 녹색 (#22C55E)
- 주황색 (#F97316)
- 보라색 (#A855F7)

**3) 도형 크기**:
- 도형 형태에 따라 자동 조정
- Gantt Bar: 시작/종료 날짜에 따라 가로 길이 자동
- 기타 도형: 고정 크기 (예: 40x40px)

#### 2.3.5 적용 방식

**옵션 1: 실시간 적용** (권장):
- 설정 변경 시 **즉시** Gantt Chart의 모든 Task에 반영
- 미리보기와 실제 Gantt가 항상 동기화

**옵션 2: 적용 버튼**:
- [Apply] 버튼 제공
- 버튼 클릭 시 Gantt Chart에 일괄 적용
- 설정 중 실제 Chart는 변경되지 않음

#### 2.3.6 저장 및 복원

**저장**:
- 도형 형태
- 도형 색상
- 선택된 속성 목록
- 각 속성의 라벨 텍스트
- 각 속성의 위치 좌표 (x, y)

**복원**:
- 저장된 설정을 불러와 동일하게 재현

---

## 3. Search Filter Section (NEW)

### 3.1 개요

**위치**:
- Gantt Configuration 섹션 바로 아래
- Gantt Chart 섹션 바로 위

**목적**:
- 사용자가 Depth 속성 기반으로 Gantt를 필터링
- 원하는 Task만 선택적으로 표시

### 3.2 UI 구조

```
┌─────────────────────────────────────────────────────┐
│  Search Filter                               [Clear] │
├─────────────────────────────────────────────────────┤
│                                                      │
│  Depth 1: Project                                    │
│  [Project A ▼] [Project B ▼] [All]                  │
│                                                      │
│  Depth 2: Phase                                      │
│  [Phase 1 ▼] [Phase 2 ▼] [All]                      │
│                                                      │
│  Depth 3: Task                                       │
│  [Search: ________________] 🔍                       │
│                                                      │
└─────────────────────────────────────────────────────┘
```

### 3.3 필터 타입

#### 3.3.1 Depth 기반 필터

**동작**:
- Structure 탭에서 정의된 **모든 Depth**에 대해 필터 제공
- 각 Depth별로 필터 컨트롤 자동 생성

**예시**:
- Depth 1: Project → [Dropdown: Project A, Project B, All]
- Depth 2: Phase → [Dropdown: Phase 1, Phase 2, All]
- Depth 3: Task → [텍스트 검색]

#### 3.3.2 필터 컨트롤 타입

**Dropdown (선택형)**:
- 속성값이 제한적일 때 (예: Project, Owner, Status)
- 다중 선택 가능 (체크박스 Dropdown)
- "All" 옵션으로 전체 선택

**텍스트 검색 (입력형)**:
- 속성값이 다양할 때 (예: Task 이름)
- 키워드 부분 일치 검색
- 실시간 필터링

### 3.4 필터 적용

**동작**:
- 필터 선택 시 **즉시** Gantt Chart에 반영
- 조건에 맞는 Task만 표시
- 조건에 맞지 않는 Task는 숨김 (collapse)

**다중 필터**:
- 여러 Depth 필터를 동시에 적용 가능
- AND 조건으로 동작 (모든 조건을 만족하는 Task만 표시)

**예시**:
```
필터 설정:
- Depth 1 (Project): "Project A" 선택
- Depth 2 (Phase): "Phase 1" 선택
- Depth 3 (Task): "UI" 검색

→ 결과: Project A의 Phase 1에 속하고 이름에 "UI"가 포함된 Task만 표시
```

### 3.5 Clear 기능

**[Clear All] 버튼**:
- 모든 필터 초기화
- 전체 Task 다시 표시

**개별 Clear**:
- 각 필터 옆에 [×] 아이콘
- 해당 필터만 해제

---

## 4. Gantt Chart Section

### 4.1 Task Drag & Drop

#### 4.1.1 기본 동작

**활성화**:
- 모든 Task는 **드래그 가능**
- 마우스 커서: grab → grabbing

**이동 방향**:
- **X축 (가로)**: 자유롭게 이동 가능
  - 시작 날짜 / 종료 날짜 변경
- **Y축 (세로)**: 제한적 이동
  - **같은 Row(그룹) 내에서만** 이동 가능
  - **다른 Row로 이동 불가** (차단)

#### 4.1.2 제약 조건

**Row 경계 제한**:
- Task를 다른 Row로 드래그 시:
  - 시각적 피드백 (빨간색 테두리, 금지 커서)
  - Drop 불가
  - 원래 위치로 스냅백

**예시**:
```
[Row 1: Project A]
  ├─ Task 1  ← 드래그 가능 (같은 Row 내)
  └─ Task 2

[Row 2: Project B]
  └─ Task 3  ← Task 1을 여기로 드래그 불가
```

#### 4.1.3 스냅 기능

**시간 스냅**:
- 드래그 시 가까운 날짜/분기/월에 자동 정렬
- 스냅 단위: Timeline 설정에 따름
  - 년도만: 년 단위 스냅
  - 분기 표시: 분기 단위 스냅
  - 월 표시: 월 단위 스냅

**Row 스냅**:
- 같은 Row 내에서 정확한 라인에 정렬

#### 4.1.4 시각적 피드백

**드래그 중**:
- Task 투명도 50%
- 그림자 효과
- Drop 가능 영역 강조

**Drop 후**:
- 부드러운 애니메이션으로 최종 위치에 배치
- 변경된 날짜 정보 업데이트

### 4.2 Task 스타일

#### 4.2.1 기본 색상

**Task Bar 색상**: 연한 회색
- 기본: `#D1D5DB` (Gray-300)
- Hover: `#9CA3AF` (Gray-400)
- Selected: `#6B7280` (Gray-500)

**단, Attributes 탭에서 색상을 지정한 경우 해당 색상 우선 적용**

#### 4.2.2 테두리 및 음영

- Border: 1px solid #9CA3AF
- Border Radius: 4px
- Box Shadow: 0 1px 2px rgba(0, 0, 0, 0.05)

#### 4.2.3 텍스트

- 폰트 크기: 12px
- 폰트 색상: #1E293B (어두운 회색)
- 폰트 굵기: 400 (Regular)

### 4.3 오늘 날짜 표기

#### 4.3.1 Today Line (오늘 날짜 선)

**표시 방법**:
- **세로 선**으로 Gantt Chart 전체를 관통
- X축의 오늘 날짜 위치에 배치

**스타일**:
- 색상: 빨간색 (#EF4444)
- 두께: 2px
- 스타일: Dashed (점선) 또는 Solid (실선)
- Z-index: Task 위에 표시

**예시**:
```
    2024 Q1      Q2      Q3      Q4
    ┃            │       ┃
    ┃   Task 1   ────────┃──────
    ┃            │       ┃
    ┃   Task 2   ──┃─────────────
                   ↑
              Today Line
              (2024-06-15)
```

#### 4.3.2 Today Label (오늘 날짜 라벨)

**표시 방법**:
- Today Line 상단에 "Today" 라벨 표시
- 또는 오늘 날짜 표시 (예: "2024-06-15")

**스타일**:
- 배경: 빨간색 (#EF4444)
- 텍스트: 흰색 (#FFFFFF)
- 폰트 크기: 10px
- Padding: 4px 8px
- Border Radius: 4px

#### 4.3.3 동적 업데이트

**자동 갱신**:
- 매일 자동으로 오늘 날짜 업데이트
- 페이지 로드 시 현재 날짜 기준으로 표시

**타임존**:
- 사용자의 로컬 타임존 기준

### 4.4 Gantt 추가 기능

#### 4.4.1 Zoom (확대/축소)

**기능** (옵션):
- Zoom In: X축 시간 범위 축소 (더 상세하게)
- Zoom Out: X축 시간 범위 확대 (더 넓게)
- 버튼 또는 마우스 휠로 제어

#### 4.4.2 스크롤

**가로 스크롤**:
- X축이 화면을 벗어날 때 자동으로 스크롤바 표시

**세로 스크롤**:
- Y축 Task가 많을 때 스크롤 가능

#### 4.4.3 Task 클릭

**기능** (옵션):
- Task 클릭 시 상세 정보 팝업/사이드패널
- 속성 값 전체 표시
- 편집 가능

---

## 5. 개발 환경 및 기술 스택

### 5.1 개발 조건

#### 백엔드
- **Runtime**: Node.js (v18 이상 권장)
- **Framework**: Express.js 또는 NestJS
- **API 스타일**: RESTful API 또는 GraphQL

#### 프론트엔드
- **Framework**: React (v18 이상)
- **언어**: JavaScript 또는 TypeScript (권장)
- **빌드 도구**: Vite, Create React App, 또는 Next.js

#### 데이터베이스
- **DBMS**: PostgreSQL (v14 이상)
- **ORM**: Prisma, Sequelize, 또는 TypeORM (옵션)

#### 추가 도구
- **상태 관리**: Redux Toolkit, Zustand, 또는 Recoil
- **HTTP 클라이언트**: Axios 또는 Fetch API
- **스타일링**: Tailwind CSS (SPEC_UI.md 기준)

---

## 6. Task 기본 속성 정의

### 8.1 속성 목록 및 데이터 타입

이 프로젝트의 Task는 다음과 같은 **18개의 기본 속성**을 가집니다:

| 순번 | 속성명 (한글) | 속성명 (영문) | 데이터 타입 | 필수 여부 |
|------|--------------|--------------|------------|---------|
| 1 | 시작일 | startDate | DATE | 필수 |
| 2 | 종료일 | endDate | DATE | 필수 |
| 3 | 제품 Type | productType | VARCHAR(50) | 필수 |
| 4 | Density | density | VARCHAR(20) | 필수 |
| 5 | 공정명 | process | VARCHAR(20) | 필수 |
| 6 | PRA일정 | praSchedule | DATE | 옵션 |
| 7 | 모공정 여부 | isMainProcess | VARCHAR(20) | 필수 |
| 8 | NPI 여부 | isNPI | VARCHAR(10) | 필수 |
| 9 | Organization | organization | VARCHAR(20) | 필수 |
| 10 | Stack 방식 | stackMethod | VARCHAR(20) | 필수 |
| 11 | Number of Stack | numberOfStack | VARCHAR(20) | 필수 |
| 12 | Number of Die | numberOfDie | VARCHAR(20) | 필수 |
| 13 | Package Size | packageSize | DECIMAL(10,2) | 옵션 |
| 14 | Package Height | packageHeight | DECIMAL(10,2) | 옵션 |
| 15 | VDD1 | vdd1 | DECIMAL(10,2) | 옵션 |
| 16 | VDD2 | vdd2 | DECIMAL(10,2) | 옵션 |
| 17 | VDDQ | vddq | DECIMAL(10,2) | 옵션 |
| 18 | Speed | speed | DECIMAL(10,2) | 옵션 |

### 8.2 속성별 허용 값 (Value Sets)

#### 날짜 타입 속성
- **시작일 (startDate)**: 임의의 날짜 (예: 2022-01-15, 2024-06-20)
- **종료일 (endDate)**: 시작일 이후의 임의의 날짜
- **PRA일정 (praSchedule)**: 임의의 날짜

#### 선택형 (Enum) 속성

**Density** (제한된 값):
- `16Gb`
- `32Gb`
- `12Gb`
- `8Gb`
- `4Gb`

**공정명 (process)**:
- `D1`
- `D2`
- `D3`

**모공정 여부 (isMainProcess)**:
- `모제품`
- `자제품`

**NPI 여부 (isNPI)**:
- `Yes`
- `No`

**Organization**:
- `x4`
- `x8`
- `x16`

**Stack 방식 (stackMethod)**:
- `TSV` (기본값)

**Number of Stack**:
- `4H`
- `8H`

**Number of Die**:
- `SDP`
- `16DP`

#### 수치형 (Decimal) 속성

다음 속성들은 **임의의 소수점 숫자** (최대 소수점 2자리):
- **Package Size**: 예) 12.50, 15.75, 20.00
- **Package Height**: 예) 1.20, 1.45, 2.00
- **VDD1**: 예) 1.10, 1.20, 1.35
- **VDD2**: 예) 1.05, 1.15, 1.25
- **VDDQ**: 예) 1.20, 1.30, 1.40
- **Speed**: 예) 3200.00, 4800.00, 6400.00

#### 제품 Type (productType)

**자유 텍스트** 형식이지만, 초기 데이터에서는 다음과 같은 예시 사용:
- `DDR5`
- `DDR6`
- `LPDDR5`
- `LPDDR5X`

### 8.3 데이터베이스 스키마

#### PostgreSQL 테이블 정의

```sql
CREATE TABLE tasks (
  id SERIAL PRIMARY KEY,

  -- 날짜 속성
  start_date DATE NOT NULL,
  end_date DATE NOT NULL,
  pra_schedule DATE,

  -- 텍스트 속성
  product_type VARCHAR(50) NOT NULL,
  density VARCHAR(20) NOT NULL,
  process VARCHAR(20) NOT NULL,
  is_main_process VARCHAR(20) NOT NULL,
  is_npi VARCHAR(10) NOT NULL,
  organization VARCHAR(20) NOT NULL,
  stack_method VARCHAR(20) NOT NULL DEFAULT 'TSV',
  number_of_stack VARCHAR(20) NOT NULL,
  number_of_die VARCHAR(20) NOT NULL,

  -- 수치 속성
  package_size DECIMAL(10,2),
  package_height DECIMAL(10,2),
  vdd1 DECIMAL(10,2),
  vdd2 DECIMAL(10,2),
  vddq DECIMAL(10,2),
  speed DECIMAL(10,2),

  -- 메타 정보
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

  -- 제약 조건
  CONSTRAINT check_dates CHECK (end_date >= start_date),
  CONSTRAINT check_density CHECK (density IN ('16Gb', '32Gb', '12Gb', '8Gb', '4Gb')),
  CONSTRAINT check_process CHECK (process IN ('D1', 'D2', 'D3')),
  CONSTRAINT check_main_process CHECK (is_main_process IN ('모제품', '자제품')),
  CONSTRAINT check_npi CHECK (is_npi IN ('Yes', 'No')),
  CONSTRAINT check_organization CHECK (organization IN ('x4', 'x8', 'x16')),
  CONSTRAINT check_number_of_stack CHECK (number_of_stack IN ('4H', '8H')),
  CONSTRAINT check_number_of_die CHECK (number_of_die IN ('SDP', '16DP'))
);

-- 인덱스 생성 (검색 성능 향상)
CREATE INDEX idx_tasks_product_type ON tasks(product_type);
CREATE INDEX idx_tasks_density ON tasks(density);
CREATE INDEX idx_tasks_start_date ON tasks(start_date);
CREATE INDEX idx_tasks_end_date ON tasks(end_date);
CREATE INDEX idx_tasks_process ON tasks(process);
```

### 8.4 초기 데이터 (Seed Data)

#### 25개 Task 샘플 데이터 생성 규칙

**분포 전략**:
- **제품 Type**: 4가지 타입 분포
  - DDR5: 6개
  - DDR6: 6개
  - LPDDR5: 7개
  - LPDDR5X: 6개

- **Density**: 5가지 밀도 고르게 분포
  - 16Gb: 5개
  - 32Gb: 5개
  - 12Gb: 5개
  - 8Gb: 5개
  - 4Gb: 5개

- **공정명**: 3가지 공정 고르게 분포
  - D1: 8개
  - D2: 9개
  - D3: 8개

- **날짜 범위**: 2022-01-01 ~ 2026-12-31 사이에 고르게 분포
  - 각 분기별로 적절히 배치
  - Task 기간: 최소 1개월 ~ 최대 1년

- **기타 속성**: 랜덤하게 선택

#### 샘플 데이터 SQL (일부 예시)

```sql
INSERT INTO tasks (
  start_date, end_date, product_type, density, process,
  pra_schedule, is_main_process, is_npi, organization,
  stack_method, number_of_stack, number_of_die,
  package_size, package_height, vdd1, vdd2, vddq, speed
) VALUES
-- Task 1: LPDDR5, 16Gb, D1
('2022-01-15', '2022-06-30', 'LPDDR5', '16Gb', 'D1',
 '2022-02-01', '모제품', 'Yes', 'x4',
 'TSV', '4H', 'SDP',
 12.50, 1.20, 1.10, 1.05, 1.20, 3200.00),

-- Task 2: LPDDR5X, 32Gb, D2
('2022-03-01', '2022-09-30', 'LPDDR5X', '32Gb', 'D2',
 '2022-04-15', '자제품', 'No', 'x8',
 'TSV', '8H', '16DP',
 15.75, 1.45, 1.20, 1.15, 1.30, 4800.00),

-- Task 3: DDR5, 12Gb, D3
('2022-06-01', '2023-01-31', 'DDR5', '12Gb', 'D3',
 '2022-07-01', '모제품', 'Yes', 'x16',
 'TSV', '4H', 'SDP',
 18.00, 1.60, 1.25, 1.20, 1.35, 5600.00),

-- ... (총 25개)
;
```

**전체 25개 Task 데이터는 별도의 `seed.sql` 파일로 제공될 예정**

### 8.5 Gantt Table 초기 구성

#### Timeline 설정 (기본값)

```javascript
{
  startYear: 2022,
  endYear: 2026,  // 2022년부터 5년 (2022~2026)
  showQuarters: true,  // 분기 표시 ON
  showMonths: false    // 월 표시 OFF
}
```

**X축 표시 형태**:
```
2022          2023          2024          2025          2026
Q1 Q2 Q3 Q4  Q1 Q2 Q3 Q4  Q1 Q2 Q3 Q4  Q1 Q2 Q3 Q4  Q1 Q2 Q3 Q4
```

#### Structure 설정 (기본값)

**1개 그룹만 설정**:

```javascript
{
  rows: [
    {
      id: "row-1",
      name: "제품별 구조",
      depths: [
        { level: 1, attribute: "productType" },  // Depth 1: 제품 Type
        { level: 2, attribute: "density" }       // Depth 2: Density
      ]
    }
  ]
}
```

**Gantt Y축 계층 구조 (예시)**:

```
제품별 구조
├─ LPDDR5 (제품 Type)
│   ├─ 16Gb (Density)
│   │   ├─ Task 1
│   │   └─ Task 5
│   ├─ 32Gb
│   │   └─ Task 9
│   └─ 8Gb
│       └─ Task 13
├─ LPDDR5X
│   ├─ 32Gb
│   │   ├─ Task 2
│   │   └─ Task 6
│   └─ 4Gb
│       └─ Task 10
├─ DDR5
│   ├─ 12Gb
│   │   ├─ Task 3
│   │   └─ Task 7
│   └─ 8Gb
│       └─ Task 11
├─ HBM3
│   ├─ 16Gb
│   │   ├─ Task 4
│   │   └─ Task 8
│   └─ 32Gb
│       └─ Task 12
└─ GDDR6
    ├─ 4Gb
    │   ├─ Task 14
    │   └─ Task 18
    └─ 16Gb
        └─ Task 22
```

#### 25개 Task 배치 전략

**분포 원칙**:
1. **제품 Type별 균등 배치**: 각 제품 Type(5개)에 5개씩 Task 배치
2. **Density별 균등 배치**: 각 Density(5개)에 5개씩 Task 배치
3. **시간 분산**: 2022~2026년 5년간 고르게 분산
4. **분기별 분산**: 각 분기마다 1~2개씩 Task 시작

**배치 예시**:

| Task ID | 제품 Type | Density | 시작일 | 종료일 | 배치 분기 |
|---------|-----------|---------|--------|--------|----------|
| Task 1 | LPDDR5 | 16Gb | 2022-01-15 | 2022-06-30 | 2022 Q1 |
| Task 2 | LPDDR5X | 32Gb | 2022-03-01 | 2022-09-30 | 2022 Q1 |
| Task 3 | DDR5 | 12Gb | 2022-06-01 | 2023-01-31 | 2022 Q2 |
| Task 4 | HBM3 | 16Gb | 2022-09-01 | 2023-03-31 | 2022 Q3 |
| Task 5 | LPDDR5 | 16Gb | 2022-12-01 | 2023-06-30 | 2022 Q4 |
| ... | ... | ... | ... | ... | ... |
| Task 25 | GDDR6 | 4Gb | 2026-09-01 | 2026-12-31 | 2026 Q3 |

**전체 25개 Task의 상세 배치는 `seed.sql` 또는 `initial_tasks.json` 파일로 제공**

#### Attributes 설정 (기본값)

**초기 표시 속성** (3개 선택):
```javascript
{
  selectedAttributes: ["productType", "density", "process"],
  shape: "gantt",  // Gantt Bar 형태
  color: "#D1D5DB",  // 연한 회색 (기본)
  labelPositions: {
    productType: { x: 10, y: 10 },
    density: { x: 10, y: 25 },
    process: { x: 10, y: 40 }
  }
}
```

**Task 표시 형태**:
```
┌─────────────────────────────┐
│ ① LPDDR5                    │
│ ② 16Gb                      │
│ ③ D1                        │
└─────────────────────────────┘
```

---

## 11. 데이터 모델 (업데이트)

### 7.1 Task 객체 구조 (확장)

**새로운 18개 속성을 반영한 Task 객체**:

```javascript
{
  id: "task-001",

  // 필수 날짜 속성
  startDate: "2022-01-15",
  endDate: "2022-06-30",

  // 필수 속성
  productType: "LPDDR5",
  density: "16Gb",
  process: "D1",
  isMainProcess: "모제품",
  isNPI: "Yes",
  organization: "x4",
  stackMethod: "TSV",
  numberOfStack: "4H",
  numberOfDie: "SDP",

  // 선택 속성 (날짜)
  praSchedule: "2022-02-01",

  // 선택 속성 (수치)
  packageSize: 12.50,
  packageHeight: 1.20,
  vdd1: 1.10,
  vdd2: 1.05,
  vddq: 1.20,
  speed: 3200.00,

  // 시각화 설정
  shape: "gantt",         // gantt | circle | rectangle | triangle | diamond
  color: "#D1D5DB",       // 기본 연한 회색

  // 속성 라벨 위치 (Attributes 탭에서 설정)
  labelPositions: {
    productType: { x: 10, y: 10 },
    density: { x: 10, y: 25 },
    process: { x: 10, y: 40 }
  }
}
```

**데이터베이스 ↔ 프론트엔드 매핑**:

| DB 컬럼명 | JSON 속성명 | 타입 |
|-----------|-------------|------|
| start_date | startDate | Date |
| end_date | endDate | Date |
| product_type | productType | String |
| density | density | String |
| process | process | String |
| pra_schedule | praSchedule | Date |
| is_main_process | isMainProcess | String |
| is_npi | isNPI | String |
| organization | organization | String |
| stack_method | stackMethod | String |
| number_of_stack | numberOfStack | String |
| number_of_die | numberOfDie | String |
| package_size | packageSize | Number |
| package_height | packageHeight | Number |
| vdd1 | vdd1 | Number |
| vdd2 | vdd2 | Number |
| vddq | vddq | Number |
| speed | speed | Number |
```

### 7.2 Configuration 저장 구조 (업데이트)

**초기 구성 (기본값)**:

```javascript
{
  timeline: {
    startYear: 2022,
    endYear: 2026,  // 5년 범위
    showQuarters: true,  // 분기 표시
    showMonths: false    // 월 표시 OFF
  },
  structure: {
    rows: [
      {
        id: "row-1",
        name: "제품별 구조",
        depths: [
          { level: 1, attribute: "productType" },  // 제품 Type
          { level: 2, attribute: "density" }       // Density
        ]
      }
    ]
  },
  attributes: {
    selectedAttributes: ["productType", "density", "process"],
    shape: "gantt",
    color: "#D1D5DB",  // 연한 회색
    labelPositions: {
      productType: { x: 10, y: 10 },
      density: { x: 10, y: 25 },
      process: { x: 10, y: 40 }
    }
  },
  filters: {
    productType: [],
    density: [],
    process: []
  }
}
```

---

## 12. 기술적 고려사항

### 8.1 성능

#### Task 수가 많을 때
- **가상 스크롤링** (Virtual Scrolling) 적용
- 보이는 영역의 Task만 렌더링
- 권장 라이브러리: react-window, react-virtualized

#### Drag & Drop 최적화
- Throttle 적용 (16ms)
- GPU 가속 (transform 사용)

### 8.2 상태 관리

#### 복잡한 상태
- Redux, Zustand, Recoil 등 상태 관리 라이브러리 권장
- Configuration, Tasks, Filters를 별도 Store로 관리

### 8.3 Gantt 차트 렌더링

#### 권장 라이브러리
- **DHTMLX Gantt** (상용)
- **Frappe Gantt** (오픈소스)
- **BryntumGantt** (상용)
- 또는 **D3.js / SVG**로 직접 구현

#### SVG vs Canvas
- **SVG**: 인터랙션이 중요할 때 (드래그, 클릭)
- **Canvas**: Task 수가 매우 많을 때 (성능)

### 8.4 반응형

#### 브레이크포인트
- Desktop: 1024px 이상
- Tablet: 768px ~ 1023px
- Mobile: 767px 이하 (Gantt는 가로 스크롤 권장)

---

## 11. 우선순위 및 단계별 구현

### Phase 1: 기본 기능 (MVP)
1. ✅ Summary Rich Text Editor
2. ✅ Timeline 탭 (년도 범위, 분기/월 표시)
3. ✅ Structure 탭 (단일 Row, 최대 3개 Depth)
4. ✅ Attributes 탭 (속성 선택, 기본 Gantt Bar)
5. ✅ 기본 Gantt Chart 렌더링
6. ✅ 오늘 날짜 표기

### Phase 2: 고급 기능
1. ✅ Structure 다중 Row 지원
2. ✅ Attributes 미리보기 및 라벨 위치 조정
3. ✅ Task 도형/색상 변경
4. ✅ Task Drag & Drop (같은 Row 내)
5. ✅ Search Filter Section

### Phase 3: 최적화 및 UX 개선
1. ✅ 가상 스크롤링
2. ✅ Zoom In/Out
3. ✅ Task 상세 정보 팝업
4. ✅ 애니메이션 효과
5. ✅ 반응형 디자인

---

## 12. 예외 처리 및 검증

### 8.1 입력 검증

#### Timeline
- 시작 년도 ≤ 종료 년도
- 최소 1년 이상 범위

#### Structure
- 최소 1개 Row 필수
- 각 Row는 최소 1개 Depth 필수
- 동일 속성 중복 선택 방지 (같은 Row 내)

#### Attributes
- 최대 8개 속성 선택 제한
- 라벨 텍스트 길이 제한 (최대 20자)

### 8.2 에러 메시지

**형식**:
- 토스트 알림 (우측 상단)
- 빨간색 테두리 + 메시지

**예시**:
- "시작 년도는 종료 년도보다 작거나 같아야 합니다."
- "최소 1개의 Depth를 선택해야 합니다."
- "최대 8개까지만 선택 가능합니다."

---

## 11. 접근성 (Accessibility)

### 9.1 키보드 내비게이션
- Tab: 포커스 이동
- Enter/Space: 선택/활성화
- Arrow Keys: Dropdown 내비게이션

### 9.2 스크린 리더
- ARIA 라벨 추가
- 의미 있는 alt 텍스트
- Role 속성 지정

---

## 12. 테스트 계획

### 10.1 단위 테스트
- 각 탭의 설정 변경 로직
- 필터링 로직
- Drag & Drop 제약 조건

### 10.2 통합 테스트
- Timeline 변경 → Gantt X축 반영
- Structure 변경 → Gantt Y축 재구성
- Attributes 변경 → Task 스타일 적용

### 10.3 E2E 테스트
- 사용자 시나리오별 전체 흐름 테스트
- 다양한 브라우저 호환성

---

## 📎 참고 문서

- `SPEC_UI.md` - UI/UX 디자인 스펙 (Option B)
- `draft_req.txt` - 초기 요구사항

---

**문서 버전**: 1.0
**최종 수정일**: 2025-12-04
**작성자**: FlexibleGantt Team
**승인 대기 중**
